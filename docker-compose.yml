services:
  mpvm-client:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      MPVM_DATABASE_URL: "${MPVM_DATABASE_URL:-postgresql://mpvm:mpvm@postgres:5432/mpvm}"
      MPVM_ASSET_CARD_REQUEST_WORKERS: "${MPVM_ASSET_CARD_REQUEST_WORKERS:-4}"
      MPVM_SCAN_POSTPROCESS_WORKERS: "${MPVM_SCAN_POSTPROCESS_WORKERS:-1}"
      MPVM_SCAN_ASSET_PROCESS_WORKERS: "${MPVM_SCAN_ASSET_PROCESS_WORKERS:-1}"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./exports:/app/exports
      - ./output:/app/output
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    ports:
      - "55432:5432"
    environment:
      POSTGRES_DB: "${POSTGRES_DB:-mpvm}"
      POSTGRES_USER: "${POSTGRES_USER:-mpvm}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-mpvm}"
    volumes:
      - mpvm_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20
    restart: unless-stopped

volumes:
  mpvm_postgres:
